{erl_opts, [debug_info]}.

{deps, []}.

{relx, [{release, {worm_mesh, "1.0.0"},
         [worm_mesh]},
        {mode, prod}]}.

{profiles, [
    {test, [
        {erl_opts, [debug_info, nowarn_export_all]}
    ]}
]}.

{cover_enabled, true}.
{cover_opts, [verbose]}.
