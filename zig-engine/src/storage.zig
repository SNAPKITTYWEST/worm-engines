// Storage: Append-only file operations
const std = @import("std");

pub const Storage = struct {
    pub fn append(_: anytype, _: anytype) !void {
        // TODO: Implement append-only storage
    }
};
